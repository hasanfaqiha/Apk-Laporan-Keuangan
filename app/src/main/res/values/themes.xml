<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme (the Compose UI overrides colors at runtime). -->
    <style name="Theme.Keuanganku" parent="android:Theme.DeviceDefault.NoActionBar" />
</resources>
